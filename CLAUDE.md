# Expense Reimbursement Packager

Use `SKILL.md` as the primary instructions for reimbursement tasks. Run the deterministic scripts instead of recreating spreadsheet, PDF, invoice, or verification logic manually. See `references/platforms.md` for Windows and macOS setup.
